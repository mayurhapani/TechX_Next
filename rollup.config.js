export default {
  input: "src/index.js", // Specify the path to your entry module
  output: {
    file: "dist/bundle.js", // Specify the output file
    format: "iife", // Specify the format (e.g., 'iife' for a standalone script)
  },
};
